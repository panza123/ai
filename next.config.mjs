/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.pexels.com', 'https://www.pexels.com','images.pexels.com','media.istockphoto.com']
    }
};

export default nextConfig;
